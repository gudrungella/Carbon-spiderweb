# Secrets — never commit
.env

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Data folder — local only, never committed
data/

# Archive folder — kept locally, not committed
archive/

# Development/test scripts — kept locally, not committed
seed_from_report.py
